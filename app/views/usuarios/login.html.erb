<%= form_for(:session, url: sessions_path) do |f| %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.label "Remember me" %>			<!-- REVISAR NO SE MUESTRA EN EL SISTEMA -->
  <%= f.check_box :remember_session %>

  <%= f.submit "Iniciar sesión" %>
<% end %>
